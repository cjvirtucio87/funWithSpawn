{
  "name": "hw-process-code",
  "version": "1.0.0",
  "description": "",
  "main": "build/main.js",
  "scripts": {
    "start": "tsc && node ./build/main $SPAWNFILE",
    "bench": "tsc && node ./build/benchmark",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "promisify-node": "^0.4.0"
  }
}
